{
  "name": "react-radar-hello-world",
  "version": "1.0.0",
  "repository": "https://github.com/jaredLunde/react-radar",
  "author": "Jared Lunde",
  "license": "MIT",
  "private": false,
  "devDependencies": {
    "@jaredlunde/example-server": "^1.0.4",
    "rimraf": "^2.6.2",
    "workerize-loader": "^1.0.4"
  },
  "dependencies": {
    "react": "^16.5.0",
    "react-dom": "^16.5.0"
  },
  "scripts": {
    "start": "rimraf dist && cross-env NODE_ENV=development BABEL_ENV=development node src/server/bin",
    "start:prod": "rimraf dist && cross-env NODE_ENV=production BABEL_ENV=production node src/server/bin"
  }
}
